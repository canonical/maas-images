# Set up development environment

To set up your system for development, including installation of packages, execute:

```bash
./system-setup
```

This script will configure your development environment with all necessary dependencies for working with MAAS Images.